name: Recruitment4U Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠 Checkout Repository
        uses: actions/checkout@v4 # Updated to v4

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v4 # Updated to v4
        with:
          node-version: "20" # Recommended for Next.js 15
          cache: 'npm' # Add caching for faster builds

      - name: 📦 Install Dependencies
        run: npm ci --legacy-peer-deps # Use npm ci for reproducible builds

      - name: 🔨 Build Next.js App
        run: npm run build

      - name: � Run Tests (if applicable)
        run: npm test # Add this if you have tests

      - name: 📤 Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: next-build
          path: |
            .next
            package.json
            package-lock.json
            public
            next.config.js
            # .env should NOT be committed - use GitHub secrets instead
            node_modules # Only include if using standalone output

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Download Build Artifacts
        uses: actions/download-artifact@v4 # Updated to v4
        with:
          name: next-build
          path: ./deploy

      - name: 🚀 Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0 # More reliable than manual SSH
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/recruitment4u
            
            echo "🛑 Stopping existing application..."
            pm2 stop recruitment4u || true
            pm2 delete recruitment4u || true
            
            echo "🧹 Cleaning old files..."
            rm -rf .next
            rm -rf public
            rm -f package.json
            rm -f package-lock.json
            rm -f next.config.js
            
            echo "🚚 Moving new files..."
            mv /home/${{ secrets.SSH_USER }}/deploy/* .
            mv /home/${{ secrets.SSH_USER }}/deploy/.[^.]* .
            
            echo "📦 Installing dependencies..."
            npm ci --production
            
            echo "🔄 Restarting application..."
            pm2 start npm --name "recruitment4u" -- start
            pm2 save
            
            echo "✅ Deployment complete!"


# name: Recruitment4U Deployment

# on:
#   push:
#     branches:
#       - main # Trigger deployment on push to main branch

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: 🛠 Checkout Repository
#         uses: actions/checkout@v3

#       - name: 🔧 Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: "18"

#       - name: 📦 Install Dependencies
#         run: npm install --legacy-peer-deps

#       - name: 🔨 Build Next.js App
#         run: npm run build

#       - name: 📤 Upload Build Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: next-build
#           path: |
#             .next
#             package.json
#             package-lock.json
#             public
#             next.config.js
#             .env # Ensure your .env file is included (if applicable)

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest

#     steps:
#       - name: 📥 Download Build Artifacts
#         uses: actions/download-artifact@v3
#         with:
#           name: next-build
#           path: /tmp/recruitment4u

#       - name: 🚀 Deploy to VPS via SSH
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#           SSH_USER: ${{ secrets.SSH_USER }}
#           SSH_HOST: ${{ secrets.SSH_HOST }}
#         run: |
#           echo "$SSH_PRIVATE_KEY" > private_key.pem
#           chmod 600 private_key.pem
#           ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts

#           echo "📂 Copying build files to the server..."
#           scp -i private_key.pem -r /tmp/recruitment4u "$SSH_USER@$SSH_HOST:/var/www/recruitment4u"

#           echo "🔧 Connecting to server and setting up..."
#           ssh -i private_key.pem "$SSH_USER@$SSH_HOST" << '_
